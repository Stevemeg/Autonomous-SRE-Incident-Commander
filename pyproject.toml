[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "asic"
version = "0.3.0"
description = "Autonomous SRE Incident Commander - domain model and tenant-aware persistence"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "UNLICENSED" }

# Phase 3 scope: domain model and persistence only. No agent framework, no web
# framework, no integration clients. Those arrive with the phases that need them.
dependencies = [
    "SQLAlchemy>=2.0.23,<2.1",
    "alembic>=1.13,<2.0",
    "psycopg2-binary>=2.9.9",
    "pydantic>=2.5,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
asic = ["py.typed"]

# --------------------------------------------------------------------------- ruff

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "W",    # pycodestyle
    "F",         # pyflakes
    "I",         # isort
    "N",         # pep8-naming
    "UP",        # pyupgrade
    "B",         # bugbear
    "A",         # builtins shadowing
    "C4",        # comprehensions
    "SIM",       # simplify
    "RUF",       # ruff-specific
]
ignore = [
    "E501",   # line length is handled by the formatter
    "N818",   # exception naming: our domain errors read better without the suffix
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]
"src/asic/db/models/*" = ["A003"]  # `id`, `type` are legitimate column names

# --------------------------------------------------------------------------- mypy

[tool.mypy]
python_version = "3.11"
strict = true
warn_unreachable = true
plugins = []
mypy_path = "src"
packages = ["asic"]

[[tool.mypy.overrides]]
module = ["alembic.*"]
ignore_missing_imports = true

# -------------------------------------------------------------------------- pytest

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra --strict-markers"
markers = [
    "postgres: test requires a live PostgreSQL instance (set ASIC_TEST_DATABASE_URL)",
]
